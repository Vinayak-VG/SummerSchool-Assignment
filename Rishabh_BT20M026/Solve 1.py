import numpy as np
import math

number = 100

x = np.random.randint(2,size=n)
xcap = np.random.rand(n)

print(y)
print(ycap)

final_list = np.multiply(x,np.log2(xcap))+np.multiply(1-x,np.log2(1-xcap))
solu = (np.sum(finarr)/n)*-1

print(solu)
